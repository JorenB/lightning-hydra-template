_target_: src.models.points_module.PointsLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.0001
  weight_decay: 0.0
  verbose: true

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10

num_classes: 3

net:
  _target_: src.models.components.simple_conv_net.SimpleConvNet
  img_dim: 128
  num_classes: ${model.num_classes}

compile: false
