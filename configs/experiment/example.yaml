# @package _global_

defaults:
  - override /data: color_polygon
  - override /model: points_convnet
  - override /callbacks: default
  - override /trainer: default

tags: []

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 10
  gradient_clip_val: 0.5

model:
  optimizer:
    lr: 0.002

data:
  batch_size: 64
